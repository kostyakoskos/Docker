Основная задача - скомпилировать, протестировать и выполнить.
Каждая инструкция будет кэшироваться.
Докер образы можнго строить на основании существующих докер образов и расширять их.
FROM // базовый образ
WORKDIR // рабочая директория(меняем папку внутри контейнера)

LABEL // предоставляет мета-информацию
EVENT // кастомная информация

COPY
RUN

COPY 
RUN

// После выполнения выше команд, длл файлы готовы. 
// Можно исопльзовать не sdk образ базовый, и переиспользовать между образами какие-то результаты.
// образ состоит из слоев и каждый слой добавляет размер файлу.
// на чужой машине не должен быть установлен sdk. Doker сам доустанавливает все необходимое.